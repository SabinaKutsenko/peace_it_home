import { createAction } from "redux-act";

export const nextStepForm = createAction("next step");
export const prevStepForm = createAction("previous step");
export const changeForm = createAction("change input");
